{
  "name": "group-2",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/DeveloperWayIT/group-2.git",
  "author": "madnol <desole.manuel@gmail.com>",
  "license": "MIT",
  "scripts": {
    "server-start": "cd server && yarn start",
    "server-build": "cd server && yarn build",
    "client-install": "cd client && yarn",
    "client-build": "cd client && yarn build",
    "start-dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "heroku-postbuild": "yarn client-build"
  },

  "engines": {
    "node": "20.9.0"
    },
  
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
